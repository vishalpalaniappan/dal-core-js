export const GetSynthesisPackage = (script) => {

    /**
     * TODO:
     * This method will export the synthesis metadata for the behavior. This
     * includes the participants, the behavior name and the synthesis meta
     * data for each of the transformations in the behavior. This will then be
     * used to synthesize the behavior in the target language. Currently that
     * language is in python and the actual synthesis methods are implemented in
     * a separate tool. In the future, it will be integrated into the engine.
     *
     * TODO: I want to create a mechanism to allow the developer to register
     * effects with the engine. These effects will then become part of the
     * synthesis package and will be used in the transformations. I feel that
     * this is a clean way to integrate the scaffolding needed to enable the
     * design to interact with reality. I will explore this solution more as I
     * implement it but the core idea makes sense to me, rather than invoke the
     * behavior from the substrate level mechs, I let the behavioe invoke the
     * substrate level mechs through the effects and the effects can be swapped
     * out as needed for different substrates.
     */


}

